spring:
  datasource:
    url: jdbc:mysql://192.168.0.105:3306/cnc?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root
    driverClassName: com.mysql.jdbc.Driver
  #sql:
  #  init:
  #    schema-locations: classpath:h2-ddl-init/init-table.sql

logging:
  level:
    root: DEBUG


taco:
  orders:
    pageSize: 30